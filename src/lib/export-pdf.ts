/**
 * export-pdf.ts — MIGRÉ
 *
 * La génération PDF est désormais entièrement côté serveur via @react-pdf/renderer.
 * Voir : src/lib/pdf-template.tsx  +  src/app/api/export/[id]/route.ts (?format=pdf)
 *
 * Ce fichier n'est plus importé nulle part et peut être supprimé manuellement.
 * Il est conservé vide pour éviter des erreurs git si des branches
 * non migrées le référencent encore temporairement.
 */

// No exports — intentionally empty.
export {}
